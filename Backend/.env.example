# Este é um arquivo de exemplo. Copie-o para um arquivo chamado .env
# e preencha com suas próprias credenciais.

# Documentação de variáveis de ambiente do Prisma:
# https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Documentação sobre connection strings:
# https://pris.ly/d/connection-strings

# Substitua <YOUR_DB_USER> e <YOUR_DB_PASSWORD> por suas credenciais do MongoDB.
# O nome do banco de dados (neste caso, "Users") pode precisar ser ajustado.
DATABASE_URL="mongodb+srv://<YOUR_DB_USER>:<YOUR_DB_PASSWORD>@users.guaytaf.mongodb.net/Users?retryWrites=true&w=majority&appName=Users"

# Chave secreta para assinar os tokens JWT (JSON Web Tokens).
# Deve ser uma string aleatória, longa e segura.
JWT_SECRET="YOUR_SECRET_KEY_HERE"